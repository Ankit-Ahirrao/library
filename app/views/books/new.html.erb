<% if current_user.is_admin %>
  <h1>Add new book </h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% else %>
  Only Admins can create new book 
<%end%>