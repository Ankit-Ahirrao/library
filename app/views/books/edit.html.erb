<%= provide(:button_text, "Save changes") %>
<% if current_user.is_admin %>
  <h2>Edit book details</h2>
  <div class="row">
    <div class="col-md-4 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% else %>
  Only Admins can edit book details 
<%end%>